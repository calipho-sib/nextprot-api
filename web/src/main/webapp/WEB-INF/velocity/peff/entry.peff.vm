#foreach($isoform in $entry.isoforms)
    #set($UNESCAPED_CDATA = ${peffByIsoform.get($isoform.getIsoformAccession())})
>$UNESCAPED_CDATA$StringUtils.getCR()
$StringUtils.wrapText($isoform.getSequence(), 60)$StringUtils.getCR()
#end