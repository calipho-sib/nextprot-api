#title:Proteins associated with diseases that are associated with cardiovascular aspects
#acs:A6NCS4,O00522,O14958,O15273
#tags:disease
#count:228
select distinct ?entry  WHERE {
  ?entry :isoform /:medical / :term /:related / :childOf cv:D002318.
}
